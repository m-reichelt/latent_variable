[project]
name = "latent-variable"
version = "0.1.0"
description = ""
authors = [
    {name = "mreichelt",email = "m.reichelt@hotmail.com"}
]
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "ngsolve (>=6.2.2606,<7.0.0)",
    "anywidget (>=0.11.0,<0.12.0)",
    "matplotlib (>=3.11.1,<4.0.0)"
]

[tool.poetry]
packages = [{include = "latent_variable", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
dev = [
    "ipykernel (>=7.3.0,<8.0.0)"
]
